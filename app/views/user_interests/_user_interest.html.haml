.interest
  .interest-name
    = user_interest.interest.name
  .interest-delete
    = button_to "Delete", user_interest_path(user_interest.id), method: :delete, class: 'delete'
  .interest-content
    = render partial: 'links/link', collection: user_interest.interest.links_by_date(Date.today)